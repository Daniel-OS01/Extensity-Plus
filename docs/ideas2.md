# Extra plans

1. in the profiles.html page when configurating an extention add an option that the user can mark 'Same Page Extention', meaning that this extention is mainly will work on specific site and not run ongoing, when enabling this the two option will be available, setting manual site to trigger that rule to turn on the extention and also option for the to use the site the Extensity-Plus extention has access from the extentions' settings 'Site access'. if its too complex just let the user enter the url where the extention will turn on
2. rebuild so that profiles.html page will combine and be a centeral destination for controling and modifing the operations and functions of each extension. the task are:
  - 'URL Rules' from dashboard.html functionality should move to profiles.html - meaning that for each extention in  page the user selects the their profile and also add specific rules for the extention, like mention in section 1, user adds 'URL Rules' directly in the extention configuration in the profiles.html page, its possible to add multiple urls to trigger the extention.
  - 'Groups' from dashboard.html functionality should move to profiles.html - think about usefull way to use 'Groups' as a separate functionality then grouping the extentions into the profile.  from dashboard.html functionality should move to profiles.html - 'Groups' will not have a use if they will be in the dashboard.html page.
3. in Aliases tab in dashboard.html explantion for the feature and common use cases.
4. when hovering above text and fields the tips should be decscritive and usefull, not just the name of the field but the functionality and what does turrning on the feture mean, what a higher and lower numner practicaly mean,
